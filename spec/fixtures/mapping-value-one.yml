schema:
  type: object
  required:
    - data
  properties:
    data:
      type: object
      required:
        - example_one
      properties:
        example_one:
          type: integer
          exclusiveMinimum: 0
