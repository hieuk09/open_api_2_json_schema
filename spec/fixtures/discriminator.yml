schema:
  discriminator:
    propertyName: key
  anyOf:
  - $ref: 'mapping-value-one.yml'
  - $ref: 'mapping-value-two.yml'
  properties:
    key:
      type: string
      enum:
        - value1
        - value2
